{
  "frontend/src/App.tsx": "Main app component with TanStack Router setup, defining all routes (login, authenticated layout with dashboard/project viewer/pricing, payment success/failure). Removes unused `redirect` import.",
  "frontend/src/pages/ProjectViewer.tsx": "Full Phase 2 project viewer page with left sidebar tabs (Cladding, Accessories, Branding, Engineering), 3D BuildingSimulator canvas, toolbar with walkthrough/video-export controls; fixed by using the typed `useGetProject` hook (returns `Project`), a `useEffect` for initialising local state from backend data, correcting the navigate path to `\"/\"`, and casting `project` to `Project` where needed.",
  "frontend/src/pages/PaymentFailure.tsx": "Payment failure/cancellation page shown after a Stripe checkout is cancelled or fails. Displays an error message with options to retry (navigate to pricing) or return to dashboard.",
  "frontend/src/components/ClearanceCheckBox.tsx": "Draggable 3D vehicle clearance check box (Truck/Forklift presets) that detects collisions with structural members and changes color; fixed `setFromCamera` to use a proper `THREE.Vector2` instance instead of a plain object literal.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interaction; fixed `useCreateProject` to pass the required `engineeringInputs` and `brandingSettings` arguments (with sensible defaults) to `actor.createProject`, and added imports for the new backend types."
}